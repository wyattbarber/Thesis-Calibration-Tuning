[build-system]
requires = ["scikit-build-core", "pybind11"]
build-backend = "scikit_build_core.build"

[tool.scikit-build]
cmake.build-type = "Release"

[project]
name = "eigen_sgbm"
version = "0.0.1"

dependencies = [
]
requires-python = ">=3.11"
authors = [
    {name="Wyatt Barber", email="barberwy@mail.gvsu.edu"}
]
description = "Custom stereo block matching."